use std::experiments::test_add;
use std::experiments::make_function;

make_function!(test);

fn main() {
    let a = 1;

    let result = test_add! {
        please a
    };

    let result = result();
    println(`{result}`); // => 2
}

